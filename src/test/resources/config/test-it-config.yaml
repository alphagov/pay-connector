server:
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0

logging:
    level: INFO
    appenders:
      - type: console
        threshold: ALL
        timeZone: UTC
        target: stdout
        logFormat: "[%thread] %highlight(%-5level) %cyan(%logger{15}) - %msg %n"

links:
  frontendUrl: http://Frontend/

worldpay:
  url: http://localhost:10107/jsp/merchant/xml/paymentService.jsp
  credentials: ['username','password','merchant_id']

smartpay:
  url: http://localhost:10106/pal/servlet/soap/Payment
  notification:
    username: smartpay_test_username
    password: smartpay_test_password
  credentials: ['username','password']

executorServiceConfig:
  timeoutInSeconds: ${AUTH_READ_TIMEOUT_SECONDS:-1}
  threadsPerCpu: ${NUMBER_OF_THREADS_PER_CPU:-100}

transactionsPaginationServiceConfig:
  displayPageSize: ${TRANSACTION_LIST_DISPLAY_SIZE:-500}

jerseyClient:
  timeout: 500ms
  connectionTimeout: 500ms
  timeToLive: 1h
  cookiesEnabled: false
  maxConnections: 1024
  maxConnectionsPerRoute: 1024
  keepAlive: 0ms
  retries: 0
  userAgent: connector
  gzipEnabledForRequests: false
  proxy:
      host: localhost
      port: 1234
      scheme : http
      nonProxyHosts:
        - localhost

customJerseyClient:
  readTimeout: 90000ms
  enableProxy: false

database:
  driverClass: org.postgresql.Driver
  user: postgres
  password: mysecretpassword
  url:

  # the maximum amount of time to wait on an empty pool before throwing an exception
  maxWaitForConnection: 1s

  # the SQL query to run when validating a connection's liveness
  validationQuery: "SELECT '1'"

  # the timeout before a connection validation queries fail
  validationQueryTimeout: 3s

  # the minimum number of connections to keep open
  minSize: 8

  # the maximum number of connections to keep open
  maxSize: 32

  # whether or not idle connections should be validated
  checkConnectionWhileIdle: false

  # the amount of time to sleep between runs of the idle connection validation, abandoned cleaner and idle pool resizing
  evictionInterval: 10s

  # the minimum amount of time an connection must sit idle in the pool before it is eligible for eviction
  minIdleTime: 1 minute

jpa:
  jpaLoggingLevel: WARNING
  sqlLoggingLevel: WARNING
  ddlGenerationOutputMode: database
  queryResultsCache: false
  cacheSharedDefault: false

notifyConfig:
  emailTemplateId: ${NOTIFY_EMAIL_TEMPLATE}
  serviceId: ${NOTIFY_SERVICE_ID}
  secret: ${NOTIFY_SECRET}
  notificationBaseURL: ${NOTIFY_BASE_URL}
  emailNotifyEnabled: ${NOTIFY_EMAIL_ENABLED:-false}

