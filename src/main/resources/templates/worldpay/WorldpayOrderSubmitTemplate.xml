<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE paymentService PUBLIC "-//WorldPay//DTD WorldPay PaymentService v1//EN"
        "http://dtd.worldpay.com/paymentService_v1.dtd">
<paymentService version="1.4" merchantCode="${merchantCode}">
    <submit>
        <order orderCode="${transactionId}">
            <description>${description}</description>
            <amount currencyCode="GBP" exponent="2" value="${amount}"/>
            <paymentDetails>
                <VISA-SSL>
                    <cardNumber>${card.cardNo}</cardNumber>
                    <expiryDate>
                        <date month="${card.endDate?split('/')?first}" year="20${card.endDate?split('/')?last}"/>
                    </expiryDate>
                    <cardHolderName>${card.cardHolder}</cardHolderName>
                    <cvc>${card.cvc}</cvc>
                    <cardAddress>
                        <address>
                            <address1>${card.address.line1}</address1>
                            <#if card.address.line2??>
                            <address2>${card.address.line2}</address2>
                            </#if>
                            <postalCode>${card.address.postcode}</postalCode>
                            <city>${card.address.city}</city>
                            <#if card.address.county??>
                            <state>${card.address.county}</state>
                            </#if>
                            <countryCode>${card.address.country}</countryCode>
                        </address>
                    </cardAddress>
                </VISA-SSL>
            </paymentDetails>
        </order>
    </submit>
</paymentService>
