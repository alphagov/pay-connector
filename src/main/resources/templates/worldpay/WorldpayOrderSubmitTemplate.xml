<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE paymentService PUBLIC "-//WorldPay//DTD WorldPay PaymentService v1//EN"
        "http://dtd.worldpay.com/paymentService_v1.dtd">
<paymentService version="1.4" merchantCode="${merchantCode}">
    <submit>
        <order orderCode="${transactionId?xml}">
            <description>${description?xml}</description>
            <amount currencyCode="GBP" exponent="2" value="${amount}"/>
            <paymentDetails>
                <VISA-SSL>
                    <cardNumber>${card.cardNo}</cardNumber>
                    <expiryDate>
                        <date month="${card.endDate?split('/')?first}" year="20${card.endDate?split('/')?last}"/>
                    </expiryDate>
                    <cardHolderName>${card.cardHolder?xml}</cardHolderName>
                    <cvc>${card.cvc}</cvc>
                    <cardAddress>
                        <address>
                            <address1>${card.address.line1?xml}</address1>
                            <#if card.address.line2??>
                            <address2>${card.address.line2?xml}</address2>
                            </#if>
                            <postalCode>${card.address.postcode?xml}</postalCode>
                            <city>${card.address.city?xml}</city>
                            <#if card.address.county??>
                            <state>${card.address.county?xml}</state>
                            </#if>
                            <countryCode>${card.address.country?xml}</countryCode>
                        </address>
                    </cardAddress>
                </VISA-SSL>
            </paymentDetails>
        </order>
    </submit>
</paymentService>
